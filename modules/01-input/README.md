# 输入模块

输入模块把原始请求转为结构化简报，并在任何生成前执行三项阻断检查：是否已脱敏、是否有来源、是否具有公开/改编权限。

## 输入类型

- 案例：只接收用户自行脱敏后的摘要或摘录；
- 文章/公开材料：记录作者或机关、标题、链接、发布日期和访问日期；
- 选题想法：只能生成通用框架，不能补写“真实案例”；
- 数据复盘：记录平台导出或人工录入的真实数据与统计口径。

## 输出

生成 `input_record`、来源清单、事实/主张初分、缺口清单和路由建议。任一阻断项未解决时，状态为 `blocked`。
