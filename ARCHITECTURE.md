# 系统架构

## 设计原则

本项目是“受约束的内容工作流”，不是自动作出法律结论的应用。架构将事实、法律、表达和数据分层，防止生成模型把推测写成事实。

```text
已脱敏来源
  ↓
输入校验 → 来源锁定 → 事实/主张/缺口清单
  ↓
角度拆解（传播目标）
  ↓
内容生成（类型 × 领域 × 时长 × 人设）
  ↓
事实法律审核 → 隐私执业审核 → 口播质量审核
  ↓
人工终审 → 发布 → 实际数据复盘
```

## 五层职责

| 层 | 负责 | 不负责 |
|---|---|---|
| 输入层 | 脱敏检查、来源和授权记录 | 替用户判断材料是否可公开 |
| 拆解层 | 提取事实、争议、证据缺口、受众问题 | 补齐未提供事实 |
| 生成层 | 按参数生成口播与辅助资产 | 保证法律正确或传播效果 |
| 审核层 | 暴露风险、矛盾和待核验项 | 代替执业律师签发 |
| 数据层 | 计算已发生的指标并形成假设 | 伪造基准、归因或成功率 |

## 参数模型

传播目标与内容类型是两个维度：

- 传播目标：收藏、讨论、转发、探索、专业信任
- 内容类型：故事驱动、教育知识、对比冲击、干货实操
- 领域：民间借贷、合同纠纷、公司法、婚姻家事
- 时长：60 秒、90 秒、3 分钟
- 人设：理性务实、强硬维权、温和调解

“案源”是业务结果，受受众匹配、地域、信任、服务承接和合规等多变量影响，不作为单条脚本公式的直接承诺。

## Skill 加载

`SKILL.md` 只保留入口规则和路由；详细知识放在 `docs/`、`modules/`、`libraries/`，脚本放在 `tools/`。这种渐进加载结构减少上下文噪音并便于维护。

## 自动化边界

仓库默认支持本地文件读写和 Git 工作流。Obsidian 同步等于写入用户指定的 vault 目录；未取得路径和权限时只生成可保存文件。第三方工作台或平台发布不在 v1.0 自动化范围内。
